name: Assignment Action

on:
  push:
    branches:
      - main  # or whichever branch you are using

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2005627@ds.study.iitm.ac.in
        run: echo "Hello, world!"
